# 사용 가이드

## 🚀 빠른 시작

### 1. 환경 설정

```bash
# 가상환경 생성
python3 -m venv venv

# 가상환경 활성화
source venv/bin/activate

# 패키지 설치
pip install -r requirements.txt
```

### 2. 프로그램 실행

```bash
# 메인 프로그램 실행
python main.py
```

### 3. 사용 예시

프로그램을 실행하면 단지명을 입력하라는 메시지가 나타납니다:

```
검색할 단지명을 입력하세요 (예: 영등포 아트자이): 영등포 아트자이
```

단지명을 입력하면 다음과 같은 정보가 출력됩니다:

- **단지 기본 정보**: 단지명, 주소, 세대수, 준공년도 등
- **상세 정보**: 주차대수, 용적률, 건폐율, 난방방식 등
- **매매 내역**: 최근 매매 거래 내역
- **전세 내역**: 최근 전세 거래 내역
- **월세 내역**: 최근 월세 거래 내역
- **가격 통계**: 최고가, 최저가, 평균가, 중간가

## 📊 출력 예시

```
================================================================================
  단지정보
================================================================================
단지명         | 영등포 아트자이
주소           | 서울시 영등포구
세대수         | 1,200
동수           | 10
준공년도       | 2020
건설사         | GS건설
================================================================================

================================================================================
  매매내역
================================================================================
거래유형 | 거래일    | 층  | 전용면적 | 매매가
매매     | 2024-10   | 15  | 84.5     | 120,000
매매     | 2024-09   | 10  | 84.5     | 118,000
================================================================================
```

## 💾 데이터 저장

프로그램 실행 후 데이터를 Excel 파일로 저장할 수 있습니다:

```
💾 데이터를 파일로 저장하시겠습니까? (y/n): y
✅ 데이터가 'data/영등포_아트자이_20241119_143025.xlsx' 파일로 저장되었습니다.
```

저장된 Excel 파일은 `data/` 폴더에 저장되며, 각 시트에 다음 정보가 포함됩니다:
- 단지정보
- 상세정보
- 매매내역
- 전세내역
- 월세내역

## 🔧 개별 모듈 사용

### 크롤러만 사용하기

```python
from src.crawler import NaverRealEstateCrawler

crawler = NaverRealEstateCrawler()

# 단지 검색
results = crawler.search_complex("영등포 아트자이")

# 전체 데이터 가져오기
all_data = crawler.get_all_data("영등포 아트자이")
```

### 파서만 사용하기

```python
from src.parser import DataParser

parser = DataParser()

# 데이터 파싱
dataframes = parser.create_summary_dataframe(all_data)

# 통계 계산
stats = parser.calculate_statistics(dataframes['매매내역'], '매매가')
```

### 디스플레이만 사용하기

```python
from src.display import DataDisplay
import pandas as pd

display = DataDisplay()

# DataFrame 출력
display.print_dataframe(df, title="매매 내역")

# Excel로 저장
display.save_to_excel(dataframes, "output.xlsx")

# CSV로 저장
display.save_to_csv(df, "output.csv")
```

## ⚠️ 주의사항

### 네이버 부동산 API 제한

현재 코드는 네이버 부동산의 비공개 API를 사용합니다. 
실제 운영 환경에서는 다음 사항을 고려해야 합니다:

1. **API 엔드포인트 확인**: 네이버 부동산의 실제 API 엔드포인트를 확인하고 업데이트해야 합니다.
2. **요청 제한**: 과도한 요청은 IP 차단으로 이어질 수 있습니다.
3. **딜레이 설정**: 각 요청 사이에 적절한 딜레이를 설정하세요.
4. **이용 약관**: 네이버 부동산의 이용 약관을 준수하세요.

### 데이터 정확성

- 크롤링된 데이터는 실시간으로 변경될 수 있습니다.
- 공식 API가 아니므로 구조 변경에 따라 코드 수정이 필요할 수 있습니다.

## 🐛 문제 해결

### 검색 결과가 없을 때

```
❌ '단지명'에 대한 데이터를 찾을 수 없습니다.

💡 검색 팁:
  - 정확한 단지명을 입력해주세요
  - 지역명과 함께 입력하면 더 정확합니다 (예: 서울 영등포 아트자이)
```

### 패키지 설치 오류

```bash
# pip 업그레이드
pip install --upgrade pip

# 패키지 재설치
pip install -r requirements.txt --force-reinstall
```

### SSL 인증서 오류

```bash
# macOS의 경우
/Applications/Python\ 3.x/Install\ Certificates.command
```

## 📝 TODO 및 개선 사항

- [ ] 실제 네이버 부동산 API 엔드포인트 확인 및 업데이트
- [ ] Selenium을 사용한 동적 페이지 크롤링 구현
- [ ] 여러 단지 일괄 검색 기능
- [ ] 데이터베이스 저장 기능
- [ ] GUI 인터페이스 추가
- [ ] 가격 추이 그래프 생성
- [ ] 주변 시세 비교 기능


